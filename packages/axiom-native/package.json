{
  "name": "@axiom/axiom-native",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "pkg"
  ],
  "scripts": {
    "build": "pnpm run build:wasm && pnpm run build:ts",
    "dev": "pnpm run build:wasm && chokidar \"rust/**/*.rs\" \"rust/Cargo.toml\" -c \"pnpm run build:wasm\" --silent",
    "build:wasm": "wasm-pack build ./rust --target web --out-dir ../pkg",
    "build:ts": "tsc -p tsconfig.app.json && tsc -p tsconfig.processor.json",
    "test": "cargo test --manifest-path rust/Cargo.toml",
    "lint": "cargo fmt --manifest-path rust/Cargo.toml -- --check && cargo clippy --manifest-path rust/Cargo.toml -- -D warnings",
    "format": "cargo fmt --manifest-path rust/Cargo.toml"
  },
  "devDependencies": {
    "@types/audioworklet": "^0.0.100",
    "chokidar-cli": "^3.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.2.2"
  }
}

{
  "name": "@axiom/audio-native",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "pkg/axiom_native_bg.wasm",
    "pkg/axiom_native.js",
    "pkg/axiom_native.d.ts"
  ],
  "scripts": {
    "build": "pnpm run build:wasm && pnpm run build:ts",
    "build:wasm": "wasm-pack build ./rust --target web",
    "build:ts": "tsc"
  },
  "devDependencies": {
    "@types/audioworklet": "^0.0.100",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  }
}
